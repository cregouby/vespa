% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh_checker.R
\name{mesh_check}
\alias{mesh_check}
\title{Validate and optionally repair a triangular surface mesh}
\usage{
mesh_check(
  mesh,
  check_watertight = TRUE,
  check_intersect = TRUE,
  attempt_repair = FALSE
)
}
\arguments{
\item{mesh}{A \code{mesh3d} object.}

\item{check_watertight}{Whether to check if the mesh is closed and bounds a
volume. Default \code{TRUE}.}

\item{check_intersect}{Whether to check for self-intersections. Default
\code{TRUE}.}

\item{attempt_repair}{If \code{TRUE}, Vespa attempts to fix detected issues
(fill holes, re-orient faces, remove self-intersections). Default \code{FALSE}.}
}
\value{
A \code{mesh3d} object — identical to the input unless \code{attempt_repair = TRUE} and repairs were applied. R warnings are emitted for each issue
detected.
}
\description{
Checks a mesh for watertightness and self-intersections using CGAL's
polygon mesh processing algorithms. Issues are reported as R warnings.
When \code{attempt_repair = TRUE}, Vespa attempts to fix detected problems and
returns the repaired mesh.
}
\examples{
f <- system.file("extdata", "torus.stl", package = "vespa")
mesh <- read_stl(f)
result <- mesh_check(mesh)
}
\seealso{
Other mesh_transforms:
\code{\link[=alpha_wrapping]{alpha_wrapping()}},
\code{\link[=boolean_operation]{boolean_operation()}},
\code{\link[=isotropic_remeshing]{isotropic_remeshing()}},
\code{\link[=mesh_deformation]{mesh_deformation()}},
\code{\link[=mesh_smoothing]{mesh_smoothing()}},
\code{\link[=mesh_subdivision]{mesh_subdivision()}},
\code{\link[=patch_filling]{patch_filling()}},
\code{\link[=pca_estimate_normals]{pca_estimate_normals()}},
\code{\link[=poisson_reconstruction]{poisson_reconstruction()}},
\code{\link[=region_fairing]{region_fairing()}},
\code{\link[=shape_smoothing]{shape_smoothing()}}
}
\concept{mesh_transforms}
