% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alpha_wrapping.R
\name{vespa_alpha_wrap}
\alias{vespa_alpha_wrap}
\title{Generate a watertight mesh via alpha wrapping}
\usage{
vespa_alpha_wrap(
  mesh,
  alpha = 5,
  offset = 3,
  absolute_thresholds = FALSE,
  update_attributes = TRUE
)
}
\arguments{
\item{mesh}{A \code{mesh3d} object (input point cloud or triangle soup).}

\item{alpha}{Maximum circumradius of output faces. Smaller values produce
tighter wraps that capture finer concavities. Default \code{5} (interpreted as
5\% of the bounding-box diagonal when \code{absolute_thresholds = FALSE}).}

\item{offset}{Mesh dilatation amount. Default \code{3}.}

\item{absolute_thresholds}{If \code{TRUE}, \code{alpha} and \code{offset} are in the same
units as the mesh coordinates. If \code{FALSE} (default), they are percentages
of the bounding-box diagonal.}

\item{update_attributes}{Whether to interpolate point and cell data from the
input. Default \code{TRUE}.}
}
\value{
A \code{mesh3d} object representing the wrapped surface.
}
\description{
Constructs a 2-manifold, watertight surface mesh from a point cloud or
triangle soup using CGAL's alpha wrapping algorithm.
}
