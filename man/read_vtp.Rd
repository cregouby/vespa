% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readers.R
\name{read_vtp}
\alias{read_vtp}
\title{Read a surface mesh or point cloud from a VTP file}
\usage{
read_vtp(filename)
}
\arguments{
\item{filename}{Path to the \code{.vtp} file. The file must exist and be readable.}
}
\value{
A \code{mesh3d} object. \verb{$vb} is a 4×N matrix of homogeneous vertex
coordinates; \verb{$it} is a 3×M matrix of 1-based triangle indices.
For point-cloud VTP files (no polygon cells), \verb{$it} is a 3×0 empty
matrix.
}
\description{
Reads a \code{vtkPolyData} object from a VTK XML PolyData (\code{.vtp}) file using
VTK's XML reader. Both ASCII and binary (including binary-appended) formats
are supported.
}
\examples{
\donttest{
url <- paste0(
  "https://gitlab.kitware.com/vtk/meshing/vespa/-/raw/master/",
  "Data/Testing/hand.vtp?ref_type=heads"
)
tmp <- tempfile(fileext = ".vtp")
download.file(url, tmp, quiet = TRUE)
mesh <- read_vtp(tmp)
print(mesh)
}
}
