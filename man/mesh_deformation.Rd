% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh_deformation.R
\name{mesh_deformation}
\alias{mesh_deformation}
\title{Deform a triangular surface mesh}
\usage{
mesh_deformation(
  mesh,
  control_ids,
  target_coords,
  roi_ids = integer(0),
  mode = c("smooth", "sre_arap"),
  sre_alpha = 0.02,
  n_iterations = 5L,
  tolerance = 1e-04
)
}
\arguments{
\item{mesh}{A \code{mesh3d} object.}

\item{control_ids}{Integer vector of 1-based vertex indices that act as
handles.}

\item{target_coords}{Numeric matrix with \code{length(control_ids)} rows and 3
columns (x, y, z). Target world-space positions for each handle.}

\item{roi_ids}{Integer vector of 1-based vertex indices forming the region of
influence. Use \code{integer(0)} (the default) to let the filter determine the
region automatically.}

\item{mode}{Deformation algorithm: \code{"smooth"} (ARAP, default) or
\code{"sre_arap"} (Smoothed Rotation Enhanced ARAP).}

\item{sre_alpha}{Blending weight for SRE-ARAP (0 = pure ARAP,
1 = pure shape). Default \code{0.02}.}

\item{n_iterations}{Number of optimisation iterations. Default \code{5L}.}

\item{tolerance}{Convergence tolerance. Default \code{1e-4}.}
}
\value{
A \code{mesh3d} object with the deformed surface.
}
\description{
Deforms a triangulated surface mesh by moving a set of control vertices to
specified target positions, propagating the deformation across the surface
using CGAL's as-rigid-as-possible (ARAP) or smoothed rotation enhanced ARAP
(SRE-ARAP) algorithms.
}
\examples{
\donttest{
f <- system.file("extdata", "torus.stl", package = "vespa")
mesh <- read_stl(f)
orig   <- mesh$vb[1:3, 1]
target <- matrix(orig + c(0.1, 0, 0), nrow = 1)
result <- mesh_deformation(mesh, control_ids = 1L, target_coords = target)
}
}
