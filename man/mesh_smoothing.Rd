% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh_smoothing.R
\name{mesh_smoothing}
\alias{mesh_smoothing}
\title{Smooth a triangular surface mesh}
\usage{
mesh_smoothing(
  mesh,
  method = c("tangential", "angle_area"),
  n_iterations = 10L,
  safety_constraints = FALSE,
  update_attributes = TRUE
)
}
\arguments{
\item{mesh}{A \code{mesh3d} object.}

\item{method}{Smoothing method: \code{"tangential"} (default) for tangential
relaxation, or \code{"angle_area"} for angle and area regularisation.}

\item{n_iterations}{Number of smoothing iterations. Default \code{10L}.}

\item{safety_constraints}{If \code{TRUE}, applies safety constraints to limit
vertex displacement. Default \code{FALSE}.}

\item{update_attributes}{Whether to interpolate point and cell data from the
input. Default \code{TRUE}.}
}
\value{
A \code{mesh3d} object with the smoothed surface.
}
\description{
Smooths a triangulated surface using CGAL's polygon mesh processing
smoothing algorithms.
}
\examples{
\donttest{
f <- system.file("extdata", "torus.stl", package = "vespa")
mesh <- read_stl(f)
smooth <- mesh_smoothing(mesh, method = "tangential", n_iterations = 3L)
}
}
